apiVersion: apps/v1
kind: Deployment
metadata:
  name: vmproject-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vmproject-dev
  template:
    metadata:
      labels:
        app: vmproject-dev
    spec:
      containers:
      - name: vmproject-dev
        image: gcr.io/constellation-275522/vmproject
        ports:
        - containerPort: 80        
        resources:
          limits:
            cpu: ".5"
            memory: "500Mi"
          requests:
            cpu: ".25"
            memory: "250Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: vmproject-dev
spec:
  selector:
    app: vmproject-dev
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80    